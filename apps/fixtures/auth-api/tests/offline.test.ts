import { describe, expect, it } from "vite-plus/test";

describe("Given the auth-api fixture package", () => {
  it("when offline unit tests run, then the package is wired", () => {
    expect(true).toBe(true);
  });
});
