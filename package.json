{
  "name": "hexkit",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "ready": "vp run -r build && vp check && vp run -r test && vp run coverage",
    "dev": "vp run @hexkit/cli#dev",
    "prepare": "vp config",
    "coverage": "bash scripts/run-generator-coverage.sh"
  },
  "devDependencies": {
    "@types/node": "catalog:",
    "@vitest/coverage-v8": "catalog:",
    "vite": "catalog:",
    "vite-plus": "catalog:"
  },
  "devEngines": {
    "packageManager": {
      "name": "pnpm",
      "version": "11.18.0",
      "onFail": "download"
    }
  },
  "engines": {
    "node": ">=24.18.1"
  }
}
