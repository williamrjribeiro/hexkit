# `@hexkit/plugin-sst`

SST infrastructure generator for Hexkit. It will produce deployment
configuration for AWS Lambda, API Gateway integration, and environment handling.

This plugin turns the generated application and its adapters into a deployable
AWS project without introducing infrastructure concerns into the application
core.
