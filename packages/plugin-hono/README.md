# `@hexkit/plugin-hono`

Hono HTTP adapter generator for Hexkit. It will produce route registration,
controllers, middleware wiring, and integration with Apical-generated operations.

This plugin exposes the generated application through HTTP while keeping Hono
outside the application core.
