# `@hexkit/plugin-architecture-hexagonal`

Hexagonal architecture generator for Hexkit. It will create domain entities,
application use cases, repository ports, and service contracts.

This plugin provides the framework-independent application core that HTTP,
database, and infrastructure adapters connect to.
